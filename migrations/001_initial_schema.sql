-- Canonical starting schema. Import jeepnigo.sql only into a disposable/local
-- database after removing its INSERT statements. Production migrations must be
-- applied explicitly; web requests must not run schema repair scripts.

